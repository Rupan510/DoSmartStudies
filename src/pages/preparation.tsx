import { NavHeader } from "@/components/NavHeader";
import React from "react";

const preparation = () => {
  return (
    <div>
      <NavHeader />
      preparation
    </div>
  );
};

export default preparation;
