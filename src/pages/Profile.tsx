import { NavHeader } from "@/components/NavHeader";
import React from "react";

const Profile = () => {
  return (
    <div>
      <NavHeader />
      Profile
    </div>
  );
};

export default Profile;
